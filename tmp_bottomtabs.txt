import { NavLink } from 'react-router-dom';

const tabs = [
  { to: '/', label: 'Home', icon: 'âŒ‚' },
  { to: '/transactions', label: 'Transactions', icon: 'â‡„' },
  { to: '/budgets', label: 'Budgets', icon: 'â–¤' },
  { to: '/settings', label: 'Settings', icon: 'âš™' }
];

export default function BottomTabs() {
  return (
    <nav className="safe-bottom fixed bottom-0 left-1/2 z-40 w-full max-w-app -translate-x-1/2 border-t border-slate-200/80 bg-white/95 backdrop-blur dark:border-slate-800 dark:bg-slate-950/95">
      <div className="grid grid-cols-4">
        {tabs.map((tab) => (
          <NavLink
            key={tab.to}
            to={tab.to}
            className={({ isActive }) =>
              `flex min-h-[60px] flex-col items-center justify-center gap-1 text-[11px] font-semibold ${
                isActive ? 'text-primary' : 'text-slate-500 dark:text-slate-400'
              }`
            }
          >
            <span className="text-base">{tab.icon}</span>
            <span>{tab.label}</span>
          </NavLink>
        ))}
      </div>
    </nav>
  );
}

